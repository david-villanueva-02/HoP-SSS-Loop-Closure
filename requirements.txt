# Runtime for the SSS feature-matching report pipeline (the src/ modules + notebooks/).
#
# LightGlue and MINIMA are NOT installed from here. Clone them at the repo root and
# follow their own install steps -- see README.md, section "Setup".

# PyTorch -- match the build to your CUDA version: https://pytorch.org/get-started/
torch
torchvision

# Numerics / IO / vision
numpy
scipy
opencv-python
Pillow
matplotlib
tqdm

# Sonar + geospatial
pyxtf            # XTF side-scan sonar file parsing
pyproj           # UTM projection (EPSG:25831)
shapely

# PhysDNet inference / training helpers (src/train.py, src/inference.py)
pytorch-msssim   # SSIM metric/loss
transformers     # LR scheduler
scikit-learn

# Pulled in by the matcher backends / MINIMA
kornia
yacs
typeguard
rpds-py
